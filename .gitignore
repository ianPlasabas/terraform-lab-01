# === TERRAFORM STANDARD IGNORE ===
# Local .terraform directories
**/.terraform/*

# Terraform state files
*.tfstate
*.tfstate.backup

# Crash logs
crash.log
crash.*.log

# Sensitive variable files
*.tfvars
*.tfvars.json
*.auto.tfvars
*.auto.tfvars.json

# Override files (used for local testing)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Provider lock files — keep one globally if needed
# Usually okay to keep root .terraform.lock.hcl for consistent providers
# Ignore nested lock files in child modules/environments
env/**/.terraform.lock.hcl

# Local backend state files (S3/DynamoDB backends won’t use these)
terraform.tfstate
terraform.tfstate.backup

# CLI configuration
.terraformrc
terraform.rc

# Logs / Temp files
*.log
*.tmp
*.bak

# === OPTIONAL ===
# Ignore local environment plan outputs
*.plan

